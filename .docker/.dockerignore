# Optimized .dockerignore for infra-platform builds (from .ops/.docker/)
# Goal: Only include Dockerfile* and requirements.txt, exclude everything else
# Note: Build context is .ops/.docker/, so paths are relative to that directory

# Exclude subdirectories
ssl/

# Exclude all non-essential files in .ops/.docker/
*.md
*.yml
*.yaml
*.conf
*.sh
*.example
*.example.*
.dockerignore

# Python artifacts
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info
dist
build
.venv
venv
env
.env

# Logs
*.log
.DS_Store

# Databases
*.db
*.sqlite
*.parquet

# Testing artifacts
.pytest_cache
.coverage
htmlcov
.mypy_cache

# Git
.dockerignore
.git
.gitignore

# Documentation (not needed in image)
*.md
README*

# Example files
*.example
*.example.*

# Shell scripts (not needed in image)
*.sh

# Docker Compose files (not needed in image)
docker-compose*.yml
docker-compose*.yaml

# SSL certificates (generated at runtime)
ssl/

# Keep only what's needed:
# - Dockerfile.jenkins.base (or Dockerfile.jenkins)
# - requirements.txt
