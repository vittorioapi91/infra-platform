__pycache__
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info
dist
build
.venv
venv
env
.env
*.log
.DS_Store
*.db
*.sqlite
*.parquet
.pytest_cache
.coverage
htmlcov
.mypy_cache
.dockerignore
.git
.gitignore

# Documentation and example files (not needed in Docker build)
*.md
README*
*.example
*.example.*

# Shell scripts (not copied by Dockerfile, but exclude from context anyway)
*.sh

# Docker Compose files (not needed in Jenkins image, only Dockerfile is needed)
docker-compose*.yml
docker-compose*.yaml

# SSL certificates (generated at runtime, not needed in image)
ssl/

