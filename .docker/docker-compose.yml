# Application-specific services
# This file is for trading_agent application services only
# Infrastructure services (Airflow, Grafana, Prometheus, etc.) are in docker-compose.infra-platform.yml

version: '3.8'

services:
  # Add application-specific services here if needed in the future
  # For now, this file is kept minimal as infrastructure is separated
  # Placeholder service to make the file valid (never runs)
  _placeholder:
    image: alpine:latest
    command: echo "Application services placeholder"
    profiles:
      - never  # This service will never run, it's just to make the file valid

volumes: {}
  # Application-specific volumes can be added here

networks: {}
  # Application-specific networks can be added here
